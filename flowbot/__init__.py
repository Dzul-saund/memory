"""flowbot — инфраструктура торгового бота на 5-минутных рынках Polymarket.

Модули:

  * :mod:`flowbot.signals` — сырые данные: консенсусная цена монеты с бирж и
    книги заявок Up/Down из потока CLOB.
  * :mod:`flowbot.strategy` — ЧИСТЫЙ ЛИСТ: сюда пишется новая торговая
    логика. Сейчас `should_enter` и `should_exit` возвращают None.
  * :mod:`flowbot.engine` — базовый движок: фиды, границы раунда, цикл.
  * :mod:`flowbot.trading` — исполнение ордеров, учёт позиций, сверка с
    биржей, журнал сделок.
  * :mod:`flowbot.positions` / :mod:`flowbot.fills` — сверка с биржей и
    разбор её ответов.
  * :mod:`flowbot.stats` — журнал сделок вне папки проекта.
  * :mod:`flowbot.latency` — модель пинга.
  * :mod:`flowbot.singleton` — замок «один бот на рынок».

Прежняя стратегия удалена целиком: ни порогов, ни фильтров, ни оценок в
проекте не осталось. Инфраструктура от неё не зависела и работает как есть.
"""

__version__ = "2.0.0"
