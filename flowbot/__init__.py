"""flowbot — единая система, объединяющая два монитора в одного трейдера.

Идея (как просил пользователь):

  * «Бот 1» (fast_monitor) даёт ЦЕНУ BTC чуть раньше, чем её покажет
    Polymarket — консенсус 7 бирж + наукаст. Отсюда мы берём РЕЗКОЕ
    ДВИЖЕНИЕ цены (всплеск скорости, нормированный на σ).
  * «Бот 2» (book_monitor) даёт КНИГУ ЗАЯВОК рынка Up/Down 5m — все
    заявки, лучшие bid/ask по обеим сторонам и поток сделок. Отсюда мы
    видим «проценты» Up и Down и куда толпа двигает цену.

Логика входа/выхода живёт в :mod:`flowbot.strategy` (чистая, без I/O,
покрыта тестами). Асинхронная обвязка (фиды цен + книга + трейдер) —
в :mod:`flowbot.engine`. Модель пинга (реалистичная задержка VPS в
Цюрихе) — в :mod:`flowbot.latency`.
"""

__version__ = "1.0.0"
